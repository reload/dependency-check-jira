<?xml version="1.0"?>
<ruleset>
    <file>./bin</file>
    <file>./src</file>
    <file>./tests</file>
    <rule ref="AppocularCodingStandard"/>
    <rule ref="SlevomatCodingStandard.Files.TypeNameMatchesFileName">
      <properties>
        <!-- Tell Slevomat the root directories of our namespaces, so
             it can check if the filepath is correct. -->
	<property name="rootNamespaces" type="array">
	  <element key="src" value="DependencyCheckJira"/>
	  <element key="tests" value="DependencyCheckJira"/>
	</property>
      </properties>
    </rule>
</ruleset>
